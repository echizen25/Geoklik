<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_filter"
        android:title="Filter"
        android:icon="@drawable/ic_filter_list_24"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_search"
        android:title="Search"
        android:icon="@drawable/searchw"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_home"
        android:title="Home"
        android:icon="@drawable/ic_home_24"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_export"
        android:title="Export Selected Sites"
        android:icon="@drawable/ic_export_24"
        app:showAsAction="never"/>

    <!-- Compatibility-only resource. Gallery sync is automatic; this item stays hidden. -->
    <item
        android:id="@+id/action_sync_all"
        android:title="Sync All"
        android:visible="false"
        app:showAsAction="never"/>
</menu>
